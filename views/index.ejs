<% posts.forEach(post => { %>
    <div class="post">
        <h2><%= post.title %></h2>
        <img src="<%= post.imageUrl %>" alt="<%= post.title %>">
        <p><%= post.content %></p>
        <a href="/<%= post.id %>">Read more</a>
    </div>
<% }) %>












